import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Runs App.js and sends it to HTML
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

